# UpContent (React) SPA configuration. Copy to .env. NEVER commit .env.
# These are PUBLIC values (shipped to the browser) — no secret here.

VITE_AUTH0_DOMAIN=dev-rvvkxpvy18wueufd.us.auth0.com
# SPA client_id from Auth0 (see setup/provision_auth0.py output / README).
VITE_AUTH0_CLIENT_ID=your-spa-client-id
VITE_AUTH0_AUDIENCE=https://upcontent-api
VITE_API_BASE_URL=http://localhost:5000
